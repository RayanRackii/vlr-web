# Context packs (vlr-web)

Derived context for **frontend-specific** knowledge. **Not canonical.**

Shared/domain packs live in **`vlr-api/docs/context-packs/`**. Do not duplicate them here.

If a pack conflicts with CONTEXT / ADR / rule / code, the canonical source wins.

## How to load

1. Read [INDEX.md](./INDEX.md).
2. If the question is domain/shared, use the API INDEX instead.
3. Load only the matching pack.

No frontend-specific packs are active yet (planned on demand: `frontend-shell`, `tenant-portal-ui`, `design-system`).
