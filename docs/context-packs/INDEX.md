# Context pack index (vlr-web)

Read this to choose a pack. Do not load all packs.

## Available here

None yet. Frontend-specific packs are created on demand only.

## Canonical elsewhere (do not copy)

Domain/shared packs: `vlr-api/docs/context-packs/INDEX.md`

- `rentals` — Reservation, Slot, schedule, booking UI contracts
- `catalog` — CatalogProduct, CatalogOrder, ProductRequest, catalog UI (canonical pack on API)
- Planned on API: `platform-core`, `authentication`, `assets`

## Planned / on-demand (this repo)

- `frontend-shell` — B2B/B2C app shell, module nav
- `tenant-portal-ui` — branded portal, agenda picker
- `design-system` — tokens, loading patterns, visual conventions
